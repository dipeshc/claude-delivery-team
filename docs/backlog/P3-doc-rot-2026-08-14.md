---
by: qa (consistency-sweep)
---

# Doc rot batch (2026-08-14) — the project profile never learned about `workflows/`

**What's wrong:** Commit `91df50a` ("feat: ship a consistency-sweep workflow that
fans the spec audit out in parallel") added `workflows/consistency-sweep.js` and
updated `docs/architecture.md`, but never touched `.claude/project-profile.md`
(last edited in `7fafa43`). Four statements in the profile are stale as a result.
All four are mechanical — no judgment call, no contradiction to resolve; the
canonical side is `docs/architecture.md` plus the filesystem in every case.

**Canonical side (applies to all instances):** `docs/architecture.md` and the file
on disk. `docs/architecture.md:5-6` — "Where this document and an agent or skill
file disagree, this document is right" — and it is also the newer statement.
`git ls-files` lists `workflows/consistency-sweep.js`. This is the missed-edit
residue the charter's "Spec prose is timeless" section names.

**Evidence — every instance:**

1. **`.claude/project-profile.md:15-20`, Repo → Layout** omits `workflows/`:
   "- **Layout:** / - `.claude-plugin/` — plugin manifest. / - `agents/` — the
   team's agent definitions. / - `skills/` — the `team` and `consistency-check`
   skills. / - `docs/` — the spec: `architecture.md` (entry doc),
   `team-charter.md`, `project-profile.template.md`, and `backlog/`."
   Against `docs/architecture.md:11-13` — "Its \"code\" is the agent definitions
   under `agents/`, the skills under `skills/`, **one orchestration script under
   `workflows/`**, and the plugin manifest under `.claude-plugin/`." —
   and `docs/architecture.md:41`, which gives `workflows/consistency-sweep.js` a
   full Components-table row.
   *Fix:* add a `workflows/` bullet naming `consistency-sweep.js`.

2. **`.claude/project-profile.md:45-47`, Specification source of truth** omits it
   from the implementing set: "**Docs are spec?** Yes. `docs/architecture.md` and
   `docs/team-charter.md` specify the framework; the agent files under `agents/`
   and the skills under `skills/` implement them."
   *Fix:* name `workflows/` alongside `agents/` and `skills/`.

3. **`.claude/project-profile.md:27-28`, Quality gate → Full** scopes the audit
   surface and excludes it: "**Full:** n/a — the `consistency-check` skill
   (spec-vs-spec, then spec-vs-code over the agent/skill definitions) is this
   repo's full audit." This is the line that actually determines what phase 2
   covers, so it must be fixed too or `workflows/consistency-sweep.js` stays out
   of the audit surface even after instances 1 and 2 land.
   *Fix:* widen "the agent/skill definitions" to include the workflow script.

4. **`.claude/project-profile.md:13-14`, Package manager / runtime** is no longer
   accurate: "**Package manager / runtime:** n/a — prose-only repo (Markdown agent
   definitions, skills, and docs; no build, no runtime)." The repo now ships a
   JavaScript file executed by the workflow runtime, so "prose-only" and
   "Markdown … and docs" are false. The `n/a` field *value* is still correct —
   there is no npm and no build step.
   *Fix:* narrow the parenthetical only; do not change the field value.

**Consequence:** The profile is the one file agents read for this repo's
specifics, and it currently tells them the repo contains no `workflows/`
directory, that only agents and skills implement the spec, and that the full audit
covers agent/skill definitions. A consistency pass scoped by instance 3 skips
`workflows/consistency-sweep.js` entirely — which is how the divergence filed as
`P1-stop-consistency-sweep-legislating-audit-method` went unnoticed.

**Acceptance criteria:**

- All four instances above are corrected in one commit.
- After the change, the set of top-level directories named in
  `.claude/project-profile.md`'s Layout equals the set of tracked top-level
  directories (`git ls-files | cut -d/ -f1 | sort -u`, excluding root files).
- After the change, the implementing surfaces named in the Specification
  source-of-truth entry and in Quality gate → Full are the same set as
  `docs/architecture.md:11-13`.
- `docs/architecture.md` is **not** changed — it is the canonical side.
- Verification is a read plus that one `git ls-files` comparison. (No test suite
  exists — `.claude/project-profile.md:24-26`.)
- This item file is deleted by the merging commit.

**Exact files to change:** `.claude/project-profile.md` only — lines 13-14, 15-20,
27-28, and 45-47.

**Suggested approach:** One pass down the file, four small edits. Note that
`.claude/` is a sanctioned direct-write path for Root
(`.claude/project-profile.md:78-81`, `docs/team-charter.md:235-236`), so this can
land without a worktree if the direct lane is used — but it still needs an
explicit-path commit that also deletes this item file.
